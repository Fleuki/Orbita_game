# Orbita — прототип

Ядро механики на PixiJS + TypeScript. Все параметры — в объекте `CONFIG` в `src/main.ts`.

```bash
npm install
npm run dev     # dev-сервер
npm run build   # проверка типов + сборка в dist/
```

Управление: клик / тап / пробел — сменить направление вращения щита.
